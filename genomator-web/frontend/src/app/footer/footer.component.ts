import { Component } from '@angular/core';

@Component({
  selector: 'app-footer', // Selector used to identify this component in HTML
  templateUrl: './footer.component.html', // Path to the component's HTML template
  styleUrls: ['./footer.component.css'] // Array of paths to external CSS stylesheets
})
export class FooterComponent {

}
